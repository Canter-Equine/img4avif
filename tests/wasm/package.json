{
  "name": "img4avif-wasm-tests",
  "version": "0.5.0",
  "description": "Playwright mobile browser tests for img4avif WASM (wasm32-unknown-unknown)",
  "private": true,
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
